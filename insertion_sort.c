// insertion sort

#include <stdio.h>


void insertion_sort(int *list){

	// check first element vs rest of list
	// if first element < min of rest of list
	// swap values
	// increment to reduce list size by 1 
	// b/c first element is now in correct position
	// repeat

}


int main(){
	int unsorted[] = {3, 5, 1, 23, 16, 4};
	int sorted[6];
}